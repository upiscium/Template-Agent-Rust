version = 1

preserve_existing = [
  "Cargo.toml",
  "Cargo.lock",
  "flake.nix",
  "flake.lock",
  ".envrc",
  ".pre-commit-config.yaml",
]

[line_merge]
".gitignore" = ["/.worktrees/"]

[structured_merge]
Justfile = "agent-module-router"
"AGENTS.md" = "agent-rules-block"
