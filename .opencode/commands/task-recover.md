---
description: Recover a failed Task through the explicit model recovery policy
agent: build
---

Treat `$ARGUMENTS` as the explicit TASK FAMILY and Task/worktree context. Load the `initialize` skill, then load `task-recovery`; do not implement recovery in this command. Stop on initialization failure.
